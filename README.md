# PoC A - Java Application

Java application repository for PoC A validation.

## Configuration

Project-specific configuration is defined in `config/poc-java-app.yml`:
- Java version: 17
- Build tool: Maven
- Tests enabled
- SonarQube enabled

## CI/CD

Powered by centralized reusable workflows from `poc-central-pipelines`.

See `.github/workflows/ci.yml` for the workflow definition.
